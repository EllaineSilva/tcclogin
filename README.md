# tcclogin
Login em angular com slimframework junto composer.php

Primeiros passos é necessário ter o composer instalado, o nodejs;
rodar o comando npm install na raiz do projeto;
rodar o comando composer install na pasta api;
